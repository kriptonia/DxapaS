/**
	Show an error on top screen
*/
void error(char *s);
/**
	Show a warning on top screen
*/
void warning(char *s);
/**
	Notify to user on top screen
*/
void notify(char *s);
/**
	Show a toltip or help message on top screen
*/
void help(char *s);

/**
	Wait for vertical sync
	Check chat socket
	Check debug console socket
*/
void WaitVBL(void);

/**
	Show authors and some info about the game
*/
void credits(void);
/**
	Check if keyboard is pressed and read
*/
int read_keyboard(text *s);
/**
	Read an string from keyboard
*/
void getString(char *info, char *string, int maxlen);
/**
	Check for an string and send it to chat
*/
void sendMessage(void);
/**
	Claer top screen
*/
void clearScreen(void);

void showIp(void);

/**
	Conect to wifi
*/
void startWifi(void);

void startChat(void);

/**
Load main menu
*/
void load_menu(void);
/**
Unload menu
*/
void unload_menu(void);

/**
Create a game and listen for clients
*/
void createGame(void);

/**
Request ip from user
Connect
*/
void connectGame(void);

/**
Create a offline game 2 player 1 Nintendo DS
*/
void offlineGame(void);

/**
Load palette and background
*/
void loadMenuBg(void);


